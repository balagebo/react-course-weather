const React = require('react');

//Stateless presentational component

var About = (props) => {
    return (
        <div>
            <h3>About</h3>
            <p>About the app</p>
        </div>
    );
};

module.exports = About;
